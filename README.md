# 🛫 Telegram-бот для поиска авиабилетов и просмотра погоды в выбранных городах

## Описание проекта

Цель создания бота — помочь пользователям быстро находить и сравнивать дешёвые авиабилеты из России в популярные туристические направления. Бот решает проблему неудобного поиска: вместо открытия сайта, ввода данных и ожидания — всё происходит в одном окне Telegram.

Функционал:
- Поиск билетов туда и обратно
- Сортировка результатов по цене (дешевле/дороже)
- Отображение погоды в городах вылета и прилёта
- История поиска (сохраняется в SQLite)

Бот использует API [Aviasales](https://travelpayouts.com/) и [OpenWeatherMap](https://openweathermap.org/api).

## ### Как пользоваться

### Команды:

#### **/start** — начать работу с ботом  
Открывает главное меню с кнопками.

#### **/help** — показать справку  
Выводит подробную инструкцию по использованию.

#### **Поиск авиабилетов** (через кнопку)  
Запускает диалог:
1. Введите город вылета (например, `Москва` или `MOW`)
2. Введите город прилёта (например, `Стамбул` или `IST`)
3. Введите дату вылета (`ГГГГ-ММ-ДД`, например `2026-03-08`)
4. Введите дату возврата или отправьте `-`, если только туда

После этого бот:
- Показывает топ-3 самых дешёвых билетов
- Отображает погоду в обоих городах
- Предлагает отсортировать результаты

#### **Поиск авиабилетов** (через кнопку)  
Запускает диалог:
1. Введите город вылета (например, `Москва` или `MOW`)
2. Введите город прилёта (например, `Стамбул` или `IST`)
3. Введите дату вылета (`ГГГГ-ММ-ДД`, например `2026-03-08`)
4. Введите дату возврата или отправьте `-`, если только туда

После этого бот:
- Показывает топ-3 самых дешёвых билетов
- Отображает погоду в обоих городах
- Предлагает отсортировать результаты

bash curl "https://api.travelpayouts.com/aviasales/v3/prices_for_dates"
--get
--data-urlencode "origin=MOW"
--data-urlencode "destination=IST"
--data-urlencode "departure_at=2026-03-08"
--data-urlencode "return_at=2026-03-15"
--data-urlencode "one_way=false"
--data-urlencode "token=YOUR_TOKEN"
--data-urlencode "currency=RUB"
--data-urlencode "limit=10"
--data-urlencode "sorting=price"